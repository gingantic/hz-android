<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Allow sharing files from internal and external cache paths (needed for OTA updates) -->
    <cache-path name="cache" path="." />
    <external-cache-path name="external_cache" path="." />
</paths>
