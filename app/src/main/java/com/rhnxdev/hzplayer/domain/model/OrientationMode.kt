package com.rhnxdev.hzplayer.domain.model

/** App-wide screen orientation preference. */
enum class OrientationMode {
    /** Follow the sensor / system default. */
    AUTO,
    /** Force portrait. */
    PORTRAIT,
    /** Force landscape. */
    LANDSCAPE,
}
